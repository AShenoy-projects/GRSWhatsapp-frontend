import { HidePhoneNoPipe } from './hide-phone-no.pipe';

describe('HidePhoneNoPipe', () => {
  it('create an instance', () => {
    const pipe = new HidePhoneNoPipe();
    expect(pipe).toBeTruthy();
  });
});
